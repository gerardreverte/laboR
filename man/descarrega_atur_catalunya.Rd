% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atur.R
\name{descarrega_atur_catalunya}
\alias{descarrega_atur_catalunya}
\title{Descarrega l'atur registrat de Catalunya}
\usage{
descarrega_atur_catalunya(
  mes,
  amb_sexe = FALSE,
  variables = NULL,
  verbose = TRUE
)
}
\arguments{
\item{mes}{Mes o vector de mesos en format \code{"AAAAMM"} (ex. \code{"202501"}).
Per a un rang:
\code{format(seq(as.Date("2024-01-01"), as.Date("2024-12-01"), by = "month"), "\%Y\%m")}.}

\item{amb_sexe}{Si \code{TRUE}, desagrega les dades per sexe (afegeix la columna
\code{Sexe}). Per defecte \code{FALSE}.}

\item{variables}{Vector amb una o dues claus de variable de desagregació
(vegeu \code{\link[=llistar_variables_atur]{llistar_variables_atur()}}). Amb dues variables el servidor
retorna el producte cartesià de les categories. Per defecte \code{NULL}
(sense desagregació).}

\item{verbose}{Mostra missatges de progrés (per defecte \code{TRUE}).}
}
\value{
Un \code{data.frame} amb les columnes \code{Mes} (codi, ex. \code{202501}),
\code{Mes_etiqueta} (ex. \code{"gener de 2025"}), les columnes de desagregació si
n'hi ha, i les columnes numèriques \verb{Atur registrat},
\verb{Demandants no ocupats} i \verb{Demandants d'ocupació}. Les cel·les amb
secret estadístic (\code{"#"}) es retornen com a \code{NA}.
}
\description{
Retorna l'atur registrat, els demandants no ocupats i els demandants
d'ocupació del conjunt de Catalunya per als mesos indicats.
}
\examples{
\dontrun{
df <- descarrega_atur_catalunya(mes = "202501")
df <- descarrega_atur_catalunya(mes = "202501", variables = "edat_3")
}
}
