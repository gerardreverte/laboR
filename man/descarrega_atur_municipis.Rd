% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atur.R
\name{descarrega_atur_municipis}
\alias{descarrega_atur_municipis}
\title{Descarrega l'atur registrat per municipis}
\usage{
descarrega_atur_municipis(
  municipis,
  mes,
  amb_sexe = FALSE,
  variables = NULL,
  verbose = TRUE
)
}
\arguments{
\item{municipis}{Vector de codis INE de 5 dígits (ex. \code{"08121"}) o noms de
municipi (ex. \code{"Mataró"}). El codi INE és més robust; el nom ha de
coincidir amb el registre del servidor (vegeu \code{\link[=obtenir_municipis]{obtenir_municipis()}}).}

\item{mes}{Mes o vector de mesos en format \code{"AAAAMM"} (ex. \code{"202501"}).
Per a un rang:
\code{format(seq(as.Date("2024-01-01"), as.Date("2024-12-01"), by = "month"), "\%Y\%m")}.}

\item{amb_sexe}{Si \code{TRUE}, desagrega les dades per sexe (afegeix la columna
\code{Sexe}). Per defecte \code{FALSE}.}

\item{variables}{Vector amb una o dues claus de variable de desagregació
(vegeu \code{\link[=llistar_variables_atur]{llistar_variables_atur()}}). Amb dues variables el servidor
retorna el producte cartesià de les categories. Per defecte \code{NULL}
(sense desagregació).}

\item{verbose}{Mostra missatges de progrés (per defecte \code{TRUE}).}
}
\value{
Com \code{\link[=descarrega_atur_catalunya]{descarrega_atur_catalunya()}}, amb les columnes addicionals
\code{Municipi} (codi INE, character) i \code{Municipi_nom}.
}
\description{
Descarrega l'atur registrat per municipis
}
\section{Secret estadístic}{

En municipis de menys de 20.000 habitants, les cel·les amb valors d'1 a 3
se suprimeixen per secret estadístic i es retornen com a \code{NA}.
}

\examples{
\dontrun{
df <- descarrega_atur_municipis("08121", mes = "202501")
df <- descarrega_atur_municipis(c("08121", "08019"), mes = "202501",
                                amb_sexe = TRUE, variables = "edat_3")
}
}
