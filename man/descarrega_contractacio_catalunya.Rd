% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contractacio.R
\name{descarrega_contractacio_catalunya}
\alias{descarrega_contractacio_catalunya}
\title{Descarrega la contractació laboral de Catalunya}
\usage{
descarrega_contractacio_catalunya(
  mes,
  tipologia = "total",
  variable_files = NULL,
  variable_cols = NULL,
  verbose = TRUE
)
}
\arguments{
\item{mes}{Mes o vector de mesos en format \code{"AAAAMM"} (ex. \code{"202501"}).
El batching automàtic gestiona qualsevol nombre de mesos (màxim 12 per
crida a l'API).}

\item{tipologia}{Una de \code{"total"} (per defecte), \code{"ett"}, \code{"estrangeres"}
o \code{"estrangeres_regio"}.}

\item{variable_files}{Clau opcional d'una variable de desagregació en
files (vegeu \code{\link[=llistar_variables_contractacio]{llistar_variables_contractacio()}}).}

\item{variable_cols}{Clau opcional d'una variable en columnes: el resultat
té una columna \verb{Contractes_<categoria>} per categoria. Les variables amb
posició \code{"files"} s'intercanvien i es transposen automàticament.}

\item{verbose}{Mostra missatges de progrés (per defecte \code{TRUE}).}
}
\value{
Un \code{data.frame} amb les columnes \code{Mes} (codi, ex. \code{202501}),
\code{Mes_etiqueta}, les columnes de desagregació si n'hi ha (per a les
variables amb codi i etiqueta, \verb{<variable>_codi} i \verb{<variable>}), i la
columna numèrica \code{Contractes} (o \verb{Contractes_<categoria>} en
creuaments). Les cel·les amb secret estadístic (\code{"#"}) es retornen com
a \code{NA}.
}
\description{
Retorna el nombre de contractes registrats al conjunt de Catalunya per als
mesos indicats, per a la tipologia triada i amb desagregació opcional.
}
\examples{
\dontrun{
df <- descarrega_contractacio_catalunya(mes = "202501")
df <- descarrega_contractacio_catalunya(mes = "202501", tipologia = "ett",
                                        variable_files = "modalitat_ett")
df <- descarrega_contractacio_catalunya(mes = "202501",
                                        variable_files = "jornada",
                                        variable_cols  = "sexe")
}
}
