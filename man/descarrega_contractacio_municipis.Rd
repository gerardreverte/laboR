% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contractacio.R
\name{descarrega_contractacio_municipis}
\alias{descarrega_contractacio_municipis}
\title{Descarrega la contractació laboral per municipis}
\usage{
descarrega_contractacio_municipis(
  municipis,
  mes,
  tipologia = "total",
  variable_files = NULL,
  variable_cols = NULL,
  verbose = TRUE
)
}
\arguments{
\item{municipis}{Vector de codis INE de 5 dígits (ex. \code{"08121"}) o noms de
municipi. El batching automàtic gestiona grups de més de 20 municipis.}

\item{mes}{Mes o vector de mesos en format \code{"AAAAMM"} (ex. \code{"202501"}).
El batching automàtic gestiona qualsevol nombre de mesos (màxim 12 per
crida a l'API).}

\item{tipologia}{Una de \code{"total"} (per defecte), \code{"ett"}, \code{"estrangeres"}
o \code{"estrangeres_regio"}.}

\item{variable_files}{Clau opcional d'una variable de desagregació en
files (vegeu \code{\link[=llistar_variables_contractacio]{llistar_variables_contractacio()}}).}

\item{variable_cols}{Clau opcional d'una variable en columnes: el resultat
té una columna \verb{Contractes_<categoria>} per categoria. Les variables amb
posició \code{"files"} s'intercanvien i es transposen automàticament.}

\item{verbose}{Mostra missatges de progrés (per defecte \code{TRUE}).}
}
\value{
Com \code{\link[=descarrega_contractacio_catalunya]{descarrega_contractacio_catalunya()}}, amb les columnes
addicionals \code{Municipi} (codi INE, character) i \code{Municipi_nom}.
}
\description{
Descarrega la contractació laboral per municipis
}
\section{Secret estadístic}{

En l'àmbit municipal, les cel·les amb menys de 4 contractes se suprimeixen
per secret estadístic i es retornen com a \code{NA}. Les categories amb zero
contractes simplement no apareixen al resultat.
}

\examples{
\dontrun{
df <- descarrega_contractacio_municipis("08121", mes = "202501")
df <- descarrega_contractacio_municipis("08121", mes = "202501",
                                        variable_files = "edat_3",
                                        variable_cols  = "sexe")
}
}
